-integrer les tables fournies (tables.sql) dans une base de données test :
    *en cas d'utilisation des deux tables personne et mdp, il faut enlever le champ mdp dans personne (voir le fichier signup-check.php) et modifier les requettes du fichier login.php
    *en cas d'utilisation de la table personne seule avec le champ mdp intégré ne rien modifier (voir le fichier signup-check.php)

-db_conn.php a modifier avant les test

-signup-check.pdf a verifier la partie avec $result et $result2 dans le else


-le reste est normalement correct, mais peut très bien aussi être relue pour plus de certitude et c'est du plug and play ensuite

-ajouter a time_stamp dans personne pour savoir quand la personne s'est inscrite